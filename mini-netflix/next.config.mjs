/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['m.media-amazon.com'], // OMDb poster domain
    },
};

export default nextConfig;
